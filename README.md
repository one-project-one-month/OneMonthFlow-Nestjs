## Project setup

  

```bash

$  npm  install

```

  

## Compile and run the project

  

```bash

#prisma setup command

npx prisma migrate dev --name init
npx prisma generate

# development

$  npm  run  start

  

# watch mode

$  npm  run  dev

  

# production mode

$  npm  run  start:prod

```

  

## Run tests

  

```bash

# unit tests

$  npm  run  test

  

# e2e tests

$  npm  run  test:e2e

  

# test coverage

$  npm  run  test:cov

```
``` Table Creation Query
CREATE TABLE `TBL_MEMBER` (
    `MEMBER_ID` INTEGER NOT NULL AUTO_INCREMENT,
    `MEMBER_CODE` CHAR(36) NOT NULL DEFAULT (UUID()),
    `MEMBER_NAME` VARCHAR(191) NOT NULL,
    `GITHUB_ACCOUNT_NAME` VARCHAR(191) NULL,
    `NRC` VARCHAR(191) NULL,
    `MOBILE_NO` VARCHAR(191) NULL,
    `CREATED_DATE` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `CREATED_BY` VARCHAR(191) NULL,
    `UPDATED_DATE` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `DEL_FLAG` INTEGER NOT NULL DEFAULT 0,
    UNIQUE INDEX `TBL_MEMBER_MEMBER_CODE_key`(`MEMBER_CODE`),
    PRIMARY KEY (`MEMBER_ID`)
);

CREATE TABLE `TBL_TECHSTACK` (
    `TECHSTACK_ID` INTEGER NOT NULL AUTO_INCREMENT,
    `TECHSTACK_CODE` CHAR(36) NOT NULL DEFAULT (UUID()),
    `TECHSTACK_SHORT_CODE` VARCHAR(191) NULL,
    `TECHSTACK_NAME` VARCHAR(191) NOT NULL,
    `CREATED_DATE` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `CREATED_BY` VARCHAR(191) NULL,
    `UPDATED_DATE` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `DEL_FLAG` INTEGER NOT NULL DEFAULT 0,
    UNIQUE INDEX `TBL_TECHSTACK_TECHSTACK_CODE_key`(`TECHSTACK_CODE`),
    PRIMARY KEY (`TECHSTACK_ID`)
);

CREATE TABLE `TBL_MEMBERTECHSTACK` (
    `MEMBER_TECHSTACK_ID` INTEGER NOT NULL AUTO_INCREMENT,
    `MEMBER_CODE` CHAR(36) NOT NULL,
    `TECHSTACK_CODE` CHAR(36) NOT NULL,
    `PROFICIENCY_LEVEL` INTEGER NULL,
    `CREATED_DATE` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `CREATED_BY` VARCHAR(191) NULL,
    `UPDATED_DATE` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `DEL_FLAG` INTEGER NOT NULL DEFAULT 0,
    PRIMARY KEY (`MEMBER_TECHSTACK_ID`)
);

CREATE TABLE `TBL_TEAM` (
    `TEAM_ID` INTEGER NOT NULL AUTO_INCREMENT,
    `TEAM_CODE` CHAR(36) NOT NULL DEFAULT (UUID()),
    `TEAM_NAME` VARCHAR(191) NOT NULL,
    `CREATED_DATE` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `CREATED_BY` VARCHAR(191) NULL,
    `UPDATED_DATE` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `DEL_FLAG` INTEGER NOT NULL DEFAULT 0,
    UNIQUE INDEX `TBL_TEAM_TEAM_CODE_key`(`TEAM_CODE`),
    PRIMARY KEY (`TEAM_ID`)
);

CREATE TABLE `TBL_PROJECT` (
    `PROJECT_ID` INTEGER NOT NULL AUTO_INCREMENT,
    `PROJECT_CODE` CHAR(36) NOT NULL DEFAULT (UUID()),
    `PROJECT_NAME` VARCHAR(191) NOT NULL,
    `REPO_URL` VARCHAR(191) NULL,
    `START_DATE` DATETIME(3) NULL,
    `END_DATE` DATETIME(3) NULL,
    `PROJECT_DESCRIPTION` VARCHAR(191) NULL,
    `PROJECT_STATUS` VARCHAR(191) NULL,
    `CREATED_DATE` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `CREATED_BY` VARCHAR(191) NULL,
    `UPDATED_DATE` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `DEL_FLAG` INTEGER NOT NULL DEFAULT 0,
    UNIQUE INDEX `TBL_PROJECT_PROJECT_CODE_key`(`PROJECT_CODE`),
    PRIMARY KEY (`PROJECT_ID`)
);

CREATE TABLE `TBL_PROJECTTEAM` (
    `PROJECT_TEAM_ID` INTEGER NOT NULL AUTO_INCREMENT,
    `PROJECT_CODE` CHAR(36) NOT NULL,
    `TEAM_CODE` CHAR(36) NOT NULL,
    `PROJECT_TEAM_RANKING` DOUBLE NULL,
    `DURATION` INTEGER NULL,
    `CREATED_DATE` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `CREATED_BY` VARCHAR(191) NULL,
    `UPDATED_DATE` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `DEL_FLAG` INTEGER NOT NULL DEFAULT 0,
    PRIMARY KEY (`PROJECT_TEAM_ID`)
);

CREATE TABLE `TBL_TEAMMEMBER` (
    `TEAM_MEMBER_ID` INTEGER NOT NULL AUTO_INCREMENT,
    `TEAM_CODE` CHAR(36) NOT NULL,
    `MEMBER_CODE` CHAR(36) NOT NULL,
    `MEMBER_RATING` DOUBLE NULL,
    `CREATED_DATE` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `CREATED_BY` VARCHAR(191) NULL,
    `UPDATED_DATE` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `DEL_FLAG` INTEGER NOT NULL DEFAULT 0,
    PRIMARY KEY (`TEAM_MEMBER_ID`)
);

CREATE TABLE `TBL_PROJECTTEAMACTIVITY` (
    `PROJECT_TEAM_ACTIVITY_ID` INTEGER NOT NULL AUTO_INCREMENT,
    `MEMBER_CODE` CHAR(36) NOT NULL,
    `TEAM_CODE` CHAR(36) NOT NULL,
    `PROJECT_CODE` CHAR(36) NOT NULL,
    `TECHSTACK_CODE` CHAR(36) NOT NULL,
    `ACTIVITY_DATE` DATETIME(3) NULL,
    `TASKS` VARCHAR(191) NULL,
    `CREATED_DATE` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `CREATED_BY` VARCHAR(191) NULL,
    `UPDATED_DATE` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `DEL_FLAG` INTEGER NOT NULL DEFAULT 0,
    PRIMARY KEY (`PROJECT_TEAM_ACTIVITY_ID`)
);

```



  
